---
description: Справочник разработчика по Python API AI Translate — автоматически сгенерирован из docstring; охватывает модули core, utils, constants, CLI и MCP-сервера.
---

# Справочник разработчика

Конечным пользователям, вероятно, нужны
[страницы функций](../index.md#headline-features) или
[руководства по настройке](../setup/llm-providers.md), а не этот раздел.

Это **автоматически сгенерированный справочник API** — одна страница
на каждый Python-модуль в `src/`, отрисованная из docstring проекта.
Он предназначен для контрибьюторов и интеграторов, которые хотят
вызывать нижележащие функции из собственного Python-кода.

## Цель сборки

`uv run mkdocs build` перегенерирует эти страницы из `src/` при каждой
сборке, так что они всегда отражают текущее состояние кода.

## С чего начать

Точка входа для перевода без UI —
[`run_translation_pipeline`](api/core/translator.md) — каждая
функция десктопного приложения, CLI и MCP-сервер в конечном итоге
проходят через неё. Чтение этой функции и соседнего
`TranslationConfig` — самый быстрый способ понять pipeline.

## Структура

- **[Constants](api/constants/index.md)** — ключи настроек, коды ошибок, таблицы языков, движки i18n / темы.
- **[Core](api/core/index.md)** — pipeline перевода, диспетчер LLM, процессоры по форматам, движки OCR / STT / TTS, чекпойнты, база данных.
- **[Utils](api/utils/index.md)** — сквозные утилиты.
- **[CLI](api/cli.md)** — точка входа `ait`.
- **[MCP Server](api/mcp_server.md)** — точка входа `ait-mcp`.
